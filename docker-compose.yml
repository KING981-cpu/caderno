
services:

  app:
    build: .
    container_name: app_caderno
    ports:
      - "80:80"
    volumes:
      - .:/var/www/html
    environment:
      - DB_HOST=db
      - DB_NAME=${MYSQL_DATABASE:-caderno}
      - DB_USER=${DB_USER:-root}
      - DB_PASS=${DB_PASS:?Error: DB_PASS environment variable must be set}
      - APP_ENV=development
      - APP_DEBUG=true
      - LOG_PATH=/var/log/caderno/app.log
    depends_on:
      - db
    networks:
      - rede_sistema
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 40s

  db:
    image: mariadb:10.4
    container_name: db_caderno
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: "${MYSQL_ROOT_PASSWORD:?Error: MYSQL_ROOT_PASSWORD environment variable must be set}"
      MYSQL_DATABASE: "${MYSQL_DATABASE:-caderno}"
    volumes:
      - ./db_data:/var/lib/mysql
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - rede_sistema
    ports:
      - "3306:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "--silent"]
      interval: 10s
      timeout: 3s
      retries: 3

networks:
  rede_sistema:
    driver: bridge
